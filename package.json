{
  "name": "krishivani",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && mkdir -p ../backend/app/dist && cp -r dist/* ../backend/app/dist/"
  }
}
